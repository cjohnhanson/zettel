# Cutting a release: bump the version in Cargo.toml, commit, tag, push
# the tag. This runs from the tag.
#
# The registry's trusted publishing cannot create a crate that does not
# exist. The first publish of a name is a manual `cargo publish` with a
# token. Every release after that runs here.
name: release

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"
      - "v[0-9]+.[0-9]+.[0-9]+-*"
  # A dry run, and only ever a dry run. Every build job runs, every
  # publish job is skipped, and nothing is uploaded, so the whole matrix
  # can be exercised before a tag exists. A dispatch carries no tag, so
  # it has no version to publish and no release to attach. Publishing
  # from one reached three registries and then failed with the version
  # permanently taken.
  workflow_dispatch:

# Least privilege at the top; each job widens only what it needs.
permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  BIN_NAME: zettel

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # A tag is the only trigger, and anyone who can push a tag can
      # point one at any commit. Requiring the commit to be on main
      # means a release carries what main carries, and nothing else.
      - name: Check the tag is on main
        if: github.event_name == 'push'
        run: |
          git fetch --quiet origin main
          if ! git merge-base --is-ancestor "${GITHUB_SHA}" origin/main; then
            echo "tag ${GITHUB_REF_NAME} names a commit that is not on main" >&2
            exit 1
          fi

      # A tag that disagrees with the manifest publishes a version
      # nobody asked for, and a registry keeps it forever.
      - name: Check the tag matches Cargo.toml
        if: github.event_name == 'push'
        run: |
          manifest="v$(grep -m1 '^version' Cargo.toml | cut -d'"' -f2)"
          if [ "$manifest" != "${GITHUB_REF_NAME}" ]; then
            echo "tag ${GITHUB_REF_NAME} does not match Cargo.toml ($manifest)" >&2
            exit 1
          fi

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2

      - run: cargo fmt --check
      - run: cargo clippy --workspace --all-targets --all-features -- -D warnings
      - run: cargo test --workspace --all-features

      # The merge gate runs this suite on every pull request, so a
      # release that skipped it would be a weaker gate than the one
      # that let the commit in.
      - name: Run the missouri suite
        if: hashFiles('tests/missouri/**') != ''
        env:
          MISSOURI_SANDBOX: preinstalled
        run: |
          cargo install --git https://github.com/cjohnhanson/missouri --locked
          cd tests/missouri && missouri run

      # Catches a packaging fault while the version can still change,
      # rather than after the registry has it.
      - name: Package and build from the tarball
        run: cargo package --locked

  create-release:
    needs: verify
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      # A draft until every target uploads, so a failed build never
      # leaves a half-populated release for anyone to install from.
      - name: Open the draft release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${GITHUB_REF_NAME}" \
            --draft --verify-tag \
            --title "${GITHUB_REF_NAME}" \
            --generate-notes

  build:
    needs: [verify, create-release]
    # A dispatch skips create-release, and a skipped dependency
    # would block this job without the explicit condition.
    if: ${{ !cancelled() && needs.verify.result == 'success' }}
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
      id-token: write
      attestations: write
    strategy:
      fail-fast: false
      matrix:
        include:
          # Static musl, so one Linux artifact runs anywhere and a
          # future package needs no libc coordination.
          - target: x86_64-unknown-linux-musl
            os: ubuntu-latest
            cross: true
          - target: aarch64-unknown-linux-musl
            os: ubuntu-latest
            cross: true
          - target: x86_64-apple-darwin
            os: macos-latest
            cross: false
          - target: aarch64-apple-darwin
            os: macos-latest
            cross: false
    steps:
      - uses: actions/checkout@v4
      # rust-toolchain.toml selects the toolchain for every cargo call,
      # and the action does not read that file. A target given to the
      # action lands in `stable`, which nothing here compiles under.
      # rustup adds it to the pinned toolchain when run in the repository.
      - uses: dtolnay/rust-toolchain@stable
      - run: rustup target add ${{ matrix.target }}
      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      # cargo-zigbuild supplies the musl cross linker without a
      # container, so the matrix stays on plain runners.
      - name: Install cargo-zigbuild
        if: matrix.cross
        run: |
          pip install ziglang
          cargo install cargo-zigbuild --locked

      - name: Build
        run: |
          if [ "${{ matrix.cross }}" = "true" ]; then
            cargo zigbuild --release --locked --target ${{ matrix.target }}
          else
            cargo build --release --locked --target ${{ matrix.target }}
          fi

      - name: Assemble the archive
        id: archive
        run: |
          staging="${BIN_NAME}-${GITHUB_REF_NAME}-${{ matrix.target }}"
          mkdir -p "$staging"
          cp "target/${{ matrix.target }}/release/${BIN_NAME}" "$staging/"
          cp README.md LICENSE "$staging/"
          # The README promises the man page in every archive. The
          # target binary may not run on this runner, so a host build
          # writes it.
          mkdir -p "$staging/man"
          cargo run --release --locked --quiet --bin "${BIN_NAME}" -- gen-man "$staging/man"
          tar czf "$staging.tar.gz" "$staging"
          shasum -a 256 "$staging.tar.gz" > "$staging.tar.gz.sha256"
          echo "asset=$staging.tar.gz" >> "$GITHUB_OUTPUT"

      # Records where the binary was built and from which commit.
      # The npm job assembles its platform packages from these.
      # A GitHub release asset is a tarball, and unpacking one per
      # target in another job is more moving parts than an artifact.
      - name: Upload the raw binaries
        uses: actions/upload-artifact@v4
        with:
          name: binaries-${{ matrix.target }}
          path: |
            target/${{ matrix.target }}/release/zettel
            target/${{ matrix.target }}/release/zttl
          if-no-files-found: error

      - name: Attest the build provenance
        if: github.event_name == 'push'
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: ${{ steps.archive.outputs.asset }}

      - name: Upload to the release
        if: github.event_name == 'push'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "${GITHUB_REF_NAME}" \
            "${{ steps.archive.outputs.asset }}" \
            "${{ steps.archive.outputs.asset }}.sha256" \
            --clobber

  publish-crate:
    # Every artifact, not only this one. See publish-npm.
    needs: [build, wheels, sdist, npm-packages]
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      # `checkout` needs contents. A job that lists any permission
      # sets every other one to none, and a private repository would
      # then fail the checkout. Public repositories hide that today.
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: rust-lang/crates-io-auth-action@v1
        id: auth
      - run: cargo publish --locked
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}


  wheels:
    needs: verify
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-musl
            os: ubuntu-latest
            manylinux: musllinux_1_1
          - target: aarch64-unknown-linux-musl
            os: ubuntu-latest
            manylinux: musllinux_1_1
          - target: x86_64-apple-darwin
            os: macos-latest
          - target: aarch64-apple-darwin
            os: macos-latest
    steps:
      - uses: actions/checkout@v4
      # `--compatibility pypi` keeps a wheel's platform tag inside what
      # PyPI accepts. Without it the upload is rejected after the whole
      # matrix has already run.
      #
      # A musl-only Linux wheel does not install on a glibc interpreter,
      # so the musl builds carry a manylinux tag as well. The binary is
      # static, so one file serves both.
      - name: Build the wheel
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          manylinux: ${{ matrix.manylinux }}
          args: >-
            --release --locked --out dist
            ${{ matrix.manylinux && '--compatibility 2_17' || '' }}
            --compatibility pypi
      - name: Check both commands are in the wheel
        shell: bash
        run: |
          python3 - <<'PY'
          import glob, zipfile, sys
          names = []
          for w in glob.glob("dist/*.whl"):
              names += [n for n in zipfile.ZipFile(w).namelist() if ".data/scripts/" in n]
          want = {"zettel", "zttl"}
          have = {n.rsplit("/", 1)[-1] for n in names}
          if not want <= have:
              print(f"wheel ships {have}, missing {want - have}", file=sys.stderr)
              raise SystemExit(1)
          print("wheel ships", sorted(have))
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.target }}
          path: dist

  sdist:
    needs: verify
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: PyO3/maturin-action@v1
        with:
          command: sdist
          args: --out dist
      - uses: actions/upload-artifact@v4
        with:
          name: wheels-sdist
          path: dist

  publish-pypi:
    needs: [build, wheels, sdist, npm-packages]
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      # Trusted publishing. No token lives in this repository.
      contents: read
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: wheels-*
          path: dist
          merge-multiple: true
      - uses: astral-sh/setup-uv@v5
      - run: uv publish -v dist/*

  npm-packages:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          registry-url: "https://registry.npmjs.org"
      - uses: actions/download-artifact@v4
        with:
          pattern: binaries-*
          path: staged
      # `build` uploads one archive per target. Lay them out as
      # <dir>/<target>/<binary>, which is what the generator reads.
      - name: Lay out the built binaries
        shell: bash
        run: |
          mkdir -p bins
          for d in staged/binaries-*; do
            target="${d#staged/binaries-}"
            mkdir -p "bins/$target"
            find "$d" -type f -name "zettel" -exec cp {} "bins/$target/zettel" \;
          done
          ls -R bins
      # The generator refuses an incomplete set. A wrapper naming a
      # platform package that was never built resolves to nothing, and
      # the failure lands on whoever installs it.
      - name: Assemble the packages
        run: node npm/generate.mjs zttl zettel bins
      - name: Check the wrapper runs
        run: |
          npm pack ./npm/zttl --pack-destination /tmp
          npm pack ./npm/zttl-linux-x64-musl --pack-destination /tmp
          mkdir -p /tmp/probe && cd /tmp/probe && npm init -y >/dev/null
          npm install /tmp/*.tgz
          npx zttl --version
          npx zettel --version
      - uses: actions/upload-artifact@v4
        with:
          name: npm-packages
          path: npm

  publish-npm:
    # Every artifact this release produces, so no registry takes a
    # version while another build is still failing. A partial
    # release cannot be repaired: a registry allows a yank and
    # never a reuse.
    needs: [build, wheels, sdist, npm-packages]
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          registry-url: "https://registry.npmjs.org"
      - uses: actions/download-artifact@v4
        with:
          name: npm-packages
          path: npm
      # Platform packages first, wrapper last. A wrapper published
      # before its dependencies resolves to nothing for anyone who
      # installs in that window.
      - name: Publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          for d in npm/zttl-*; do npm publish "$d" --access public --provenance; done
          npm publish npm/zttl --access public --provenance


  debs:
    needs: build
    # No job gate. A dispatch is meant to rehearse the release, and
    # this job and `tap` were the two it skipped, so a rehearsal
    # reported green while a tag failed in both. They build on a
    # dispatch and upload only on a tag.
    runs-on: ubuntu-latest
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        target: [x86_64-unknown-linux-musl, aarch64-unknown-linux-musl]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: actions/download-artifact@v4
        with:
          pattern: binaries-${{ matrix.target }}
          path: staged
          merge-multiple: true
      - name: Install cargo-deb
        run: cargo install cargo-deb --locked
      # A .deb attached to a release installs with `dpkg -i`. It is not
      # an apt repository, so `apt-get install` still does not work.
      # That needs a signed, hosted repository, which does not exist.
      - name: Build the package
        run: |
          # --no-build packages what is already on disk, and nothing put
          # it there. The build job's artifact carries it.
          mkdir -p "target/${{ matrix.target }}/release"
          cp "staged/binaries-${{ matrix.target }}"/* \
             "target/${{ matrix.target }}/release/"
          chmod +x "target/${{ matrix.target }}/release"/*
          cargo deb --target ${{ matrix.target }} --no-build --no-strip
      - name: Attach it to the release
        if: github.event_name == 'push'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "${GITHUB_REF_NAME}" \
            target/${{ matrix.target }}/debian/*.deb --clobber


  tap:
    needs: build
    # No job gate, for the reason under `debs`.
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Check out the tap
        uses: actions/checkout@v4
        with:
          repository: cjohnhanson/homebrew-tap
          # A cross-repository push needs its own token. The default
          # GITHUB_TOKEN reaches this repository only.
          token: ${{ secrets.HOMEBREW_TAP_TOKEN }}
          path: tap
      - name: Fill in the version and the checksums
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          version="${GITHUB_REF_NAME#v}"
          formula="tap/Formula/zettel.rb"
          # A release asset is what the formula installs, so its own
          # checksum is what the formula must carry. Reading them from
          # the release means the two cannot disagree.
          gh release download "${GITHUB_REF_NAME}" --pattern '*.tar.gz' --dir dl
          declare -A slot=(
            [aarch64-apple-darwin]=PLACEHOLDER_DARWIN_ARM64
            [x86_64-apple-darwin]=PLACEHOLDER_DARWIN_X64
            [aarch64-unknown-linux-musl]=PLACEHOLDER_LINUX_ARM64
            [x86_64-unknown-linux-musl]=PLACEHOLDER_LINUX_X64
          )
          for target in "${!slot[@]}"; do
            # The archive carries the tag: build assembles
            # <bin>-<tag>-<target>. Reading it without the tag
            # found nothing, and the job exited 1 on every tag.
            file="dl/zettel-${GITHUB_REF_NAME}-${target}.tar.gz"
            if [ ! -f "$file" ]; then
              echo "no asset for ${target}" >&2
              exit 1
            fi
            sum=$(sha256sum "$file" | cut -d' ' -f1)
            sed -i "s/${slot[$target]}/${sum}/" "$formula"
          done
          sed -i "s/^  version \".*\"$/  version \"${version}\"/" "$formula"
          grep -q PLACEHOLDER "$formula" && { echo "a checksum was not filled" >&2; exit 1; } || true
      - name: Push the formula
        if: github.event_name == 'push'
        working-directory: tap
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Formula/zettel.rb
          git commit -m "zettel ${GITHUB_REF_NAME}"
          git push

  finalize:
    needs: [build, debs, tap, publish-crate, publish-pypi, publish-npm]
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Clear the draft flag
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release edit "${GITHUB_REF_NAME}" --draft=false
